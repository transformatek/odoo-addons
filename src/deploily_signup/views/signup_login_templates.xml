<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="deploily_signup_fields" inherit_id="auth_signup.fields">
        <xpath expr="//input[@id='password']" position="after">
            <small class="form-text text-primary  mb-2" style="font-size: 0.60rem;">
                Password must contain at least 8 characters, including uppercase and lowercase letters, a number, and a special character.
            </small>
            
        </xpath>
        <xpath expr="//div[@class='mb-3 field-confirm_password']" position="after">
            <div class="mb-3 field-terms d-flex align-items-start gap-2">
                <input type="checkbox"
                    name="terms_conditions"
                    id="terms_conditions"
                    class="form-check-input mt-1"
                    required="required" />
    
                <label for="terms_conditions" class="form-check-label text-muted small">
                    <t t-esc="website.terms_conditions_text"/>
                    <a t-att-href="terms_conditions_page" target="_blank">Conditions d’utilisation</a> et <a t-att-href="privacy_policy_page" target="_blank">Politique de confidentialité</a>,   et vous consentez au traitement de vos données personnelles conformément à <strong>la loi 18-07 sur la protection des individus</strong>. </label>
            </div>
        </xpath>
    </template>

    <template id="deploily_login" inherit_id="web.login" name="Log in">
        <xpath expr="//div[@class='oe_login_buttons text-center gap-1 d-grid mb-1 pt-3']" position="before">
            <t t-call="deploily_signup.recaptcha" />
        </xpath>

    </template>


    <template id="deploily_signup" inherit_id="auth_signup.signup" name="Sign up">
        <xpath expr="//div[@class='text-center oe_login_buttons d-grid pt-3']" position="before">
            <t t-call="deploily_signup.recaptcha" />
        </xpath>
    </template>
    <template id="deploily_signup.recaptcha" name="Add reCAPTCHA to Signup">
        <div class="mb-3 o_recaptcha" style="margin-top: 10px;">
            <div class="g-recaptcha" name="recaptcha" t-att-data-sitekey="website.get_recaptcha_site_key()" t-attf-data-callback="{{ callback_name or 'signup' }}"></div>
        </div>
        <script src="https://www.google.com/recaptcha/api.js" async="1" defer="1"></script>

    </template>
</odoo>